#include "Pods-Lambda-Alert.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Lambda-Alert" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/FMDB" "${PODS_ROOT}/Headers/Lambda-Alert"
OTHER_LDFLAGS = -ObjC ${PODS_LAMBDA_ALERT_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}